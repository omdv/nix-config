/**
 * Checkpoint/Rewind Extension (draft)
 *
 * NOTE: not enabled in pi settings yet.
 * This file is kept in nix-config for iteration and API-compat cleanup.
 */

import type { ExtensionAPI } from "@mariozechner/pi-coding-agent";

export default function (_pi: ExtensionAPI) {
  // Draft placeholder: tool wiring intentionally disabled until API-compat pass is completed.
}
